version: '3.8'

services:
  coturn:
    image: coturn/coturn:latest
    container_name: hyperdesk-turn-relay
    restart: always
    network_mode: host
    volumes:
      - ./turnserver.conf:/etc/coturn/turnserver.conf:ro
    command:
      - -c
      - /etc/coturn/turnserver.conf
